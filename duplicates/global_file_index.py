import os
import json
from typing import Optional

#from file_index import FileIndex

#class GlobalFileIndex(FileIndex):
#    def __init__(self, global_index_file: str = "global_index.json"):
#        super().__init__(index_file=global_index_file)
#        self.update(deep_scan=False)  # Update without deep scan on every init

